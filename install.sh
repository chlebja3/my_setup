# update ubuntu
sudo apt update && sudo apt upgrade -y

# install the basic tools
sudo apt install -y build-essential cmake python3 python3-pip git unzip wget software-properties-common curl ripgrep

# install tmux
sudo apt install tmux

# install neovim
sudo apt install neovim -y 
